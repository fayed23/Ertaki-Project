# Ertaki API (NestJS)

Default: SQLite at `ertaki.dev.sqlite` (no secrets required).

```bash
npm install
npm run start:dev
```

Listens on `http://0.0.0.0:43124/api`.

Set `DB_TYPE=postgres` and `DATABASE_URL` for PostgreSQL. See root README.
